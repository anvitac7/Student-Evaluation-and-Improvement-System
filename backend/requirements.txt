# --- Web framework ---
fastapi
uvicorn[standard]
python-multipart

# --- Config & validation ---
pydantic
pydantic-settings
email-validator

# --- Database (async MongoDB) ---
motor
pymongo

# --- Auth ---
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.0.1
google-auth
requests

# --- Rate limiting ---
slowapi

# --- Utilities ---
python-dotenv
tenacity
cloudinary

# --- Testing ---
pytest
pytest-asyncio
httpx
mongomock-motor

# --- Resume parsing (Phase 6) & OCR ---
pymupdf
pdfplumber
spacy
Pillow
# PaddleOCR and PaddlePaddle for scanned resume OCR (PP-OCRv4)
# For CPU installations: paddlepaddle
# For GPU (CUDA 11.8/12.0) installations: paddlepaddle-gpu (see README / installation guide)

# --- ML / semantic matching (Phase 7/9) ---
# Deliberately plain `transformers` + `torch`, NOT `sentence-transformers` —
# see PROJECT_PROGRESS.md Phase 7/9 section: the trained cross-encoder was
# saved in sentence-transformers 5.x's newer CrossEncoder format, which an
# older sentence-transformers install can't reliably load. Loading via
# AutoModel/AutoModelForSequenceClassification only depends on standard
# config.json + model.safetensors + tokenizer files, which are stable
# regardless of which wrapper library saved them.
torch
transformers
scikit-learn
numpy
openai
httpx
uvicorn
